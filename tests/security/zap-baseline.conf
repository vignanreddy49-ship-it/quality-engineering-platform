# OWASP ZAP baseline configuration
# The CI job runs the baseline scanner against the local ShopSphere API.
# Keep API-specific authentication/context rules here as the application adds auth.

fail_on_warn=false
